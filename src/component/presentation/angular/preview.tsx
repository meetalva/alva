import { Store } from '../../../store/store';

import { HighlightElementFunction } from '../../preview';

export function PreviewApp(store: Store, highlightElement: HighlightElementFunction): string {
	return 'angular renderer';
}
